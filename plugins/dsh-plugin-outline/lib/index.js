// Node entry for the browser-only session outline plugin.
export const name = "dsh-plugin-outline";

/** No-op Node half: the browser half in client.js does all the work. */
export function apply() {}
