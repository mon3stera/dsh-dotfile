// dsh-plugin-context entry — placeholder for Phase 1 (data layer only).
// Phases 2+ mount the engine, paragraph injector, memory system, tools, and
// Dreamer here, per docs/context-management.md.
export const name = "dsh-plugin-context";
export const inject = [];

export function apply() {}
