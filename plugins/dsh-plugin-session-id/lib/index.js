// Node entry for the browser-only session-id label plugin.
export const name = "dsh-plugin-session-id";

/** No-op Node half: the browser half in client.js does all the work. */
export function apply() {}
