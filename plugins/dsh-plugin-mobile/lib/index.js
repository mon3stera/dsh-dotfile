// Node entry for the browser-only mobile ergonomics plugin.
export const name = "dsh-plugin-mobile";

/** No-op Node half: the browser half in client.js does all the work. */
export function apply() {}
